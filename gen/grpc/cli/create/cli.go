// Code generated by goa v3.1.1, DO NOT EDIT.
//
// create gRPC client CLI support package
//
// Command:
// $ goa gen crud/design

package cli

import (
	createc "crud/gen/grpc/create/client"
	"flag"
	"fmt"
	"os"

	goa "goa.design/goa/v3/pkg"
	grpc "google.golang.org/grpc"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `create (create|list|remove|update)
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` create create --message '{
      "comments": [
         "Et facere sed.",
         "Veritatis est atque alias in.",
         "Eveniet velit nihil dignissimos."
      ],
      "id": 140759512,
      "name": "fen"
   }'` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(cc *grpc.ClientConn, opts ...grpc.CallOption) (goa.Endpoint, interface{}, error) {
	var (
		createFlags = flag.NewFlagSet("create", flag.ContinueOnError)

		createCreateFlags       = flag.NewFlagSet("create", flag.ExitOnError)
		createCreateMessageFlag = createCreateFlags.String("message", "", "")

		createListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		createRemoveFlags       = flag.NewFlagSet("remove", flag.ExitOnError)
		createRemoveMessageFlag = createRemoveFlags.String("message", "", "")

		createUpdateFlags       = flag.NewFlagSet("update", flag.ExitOnError)
		createUpdateMessageFlag = createUpdateFlags.String("message", "", "")
	)
	createFlags.Usage = createUsage
	createCreateFlags.Usage = createCreateUsage
	createListFlags.Usage = createListUsage
	createRemoveFlags.Usage = createRemoveUsage
	createUpdateFlags.Usage = createUpdateUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "create":
			svcf = createFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "create":
			switch epn {
			case "create":
				epf = createCreateFlags

			case "list":
				epf = createListFlags

			case "remove":
				epf = createRemoveFlags

			case "update":
				epf = createUpdateFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "create":
			c := createc.NewClient(cc, opts...)
			switch epn {
			case "create":
				endpoint = c.Create()
				data, err = createc.BuildCreatePayload(*createCreateMessageFlag)
			case "list":
				endpoint = c.List()
				data = nil
			case "remove":
				endpoint = c.Remove()
				data, err = createc.BuildRemovePayload(*createRemoveMessageFlag)
			case "update":
				endpoint = c.Update()
				data, err = createc.BuildUpdatePayload(*createUpdateMessageFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// createUsage displays the usage of the create command and its subcommands.
func createUsage() {
	fmt.Fprintf(os.Stderr, `The blog service gives blog details.
Usage:
    %s [globalflags] create COMMAND [flags]

COMMAND:
    create: Add new blog and return its ID.
    list: List all entries
    remove: Remove blog from storage
    update: Updating the existing blog

Additional help:
    %s create COMMAND --help
`, os.Args[0], os.Args[0])
}
func createCreateUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] create create -message JSON

Add new blog and return its ID.
    -message JSON: 

Example:
    `+os.Args[0]+` create create --message '{
      "comments": [
         "Et facere sed.",
         "Veritatis est atque alias in.",
         "Eveniet velit nihil dignissimos."
      ],
      "id": 140759512,
      "name": "fen"
   }'
`, os.Args[0])
}

func createListUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] create list

List all entries

Example:
    `+os.Args[0]+` create list
`, os.Args[0])
}

func createRemoveUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] create remove -message JSON

Remove blog from storage
    -message JSON: 

Example:
    `+os.Args[0]+` create remove --message '{
      "id": 2454953123
   }'
`, os.Args[0])
}

func createUpdateUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] create update -message JSON

Updating the existing blog
    -message JSON: 

Example:
    `+os.Args[0]+` create update --message '{
      "comments": [
         "Nulla ipsa.",
         "At sed nesciunt velit repudiandae exercitationem qui."
      ],
      "id": 3362795313,
      "name": "Non nisi."
   }'
`, os.Args[0])
}
